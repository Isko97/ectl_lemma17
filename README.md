# ectl_lemma17

experimental implementation of the algorithm described in https://arxiv.org/pdf/1412.2905.pdf - lemma 17.
Resulting tree of an E-structure as defined in Definition 24 (except it is constrained by j <= 3) is displayed in "tree.png".
Extended by an mapping to an prefix tree (saved in "trie.png")
